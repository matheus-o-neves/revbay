{
  "name": "@revbay/database",
  "version": "1.0.0",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "deploy": "prisma migrate deploy",
    "reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "tsx": "^4.20.3"
  }
}
